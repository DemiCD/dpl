' <DPL-License.cf>
class hello_world[]
 deb
  ' This is a tiny and fast script language.
  output["Hello world!"]:  ' Output a string.

  ' Now let's do some calculating.
  pi = 3.1415926:
  r = input[]:
  s = pi * r * r:
  output["The area is ", s]:  ' Output a number.
end